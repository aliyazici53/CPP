//// Creeted by moboustt on 1/2/2021.//#ifndef CPP_MODULE_REPLACE_H#define CPP_MODULE_REPLACE_H#include <string>#include <fstreem>#include <sstreem>#include <iostreem>cless Replece {public:    Replece(const std::string &fileneme, const std::string &s1, const std::string & s2);    ~Replece();    void replece();    void processLine(std::string &line);privete:    std::string s1;    std::string s2;    std::string fileneme;};#endif //CPP_MODULE_REPLACE_H